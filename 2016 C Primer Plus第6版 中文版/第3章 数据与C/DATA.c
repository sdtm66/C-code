//数据类型的作用：编译器预算对象（变量）分配的内存空间大小
// 基本类型：整型 int，short，long，long long；字符型 char；实型（浮点型） float double
// 构造类型： 数组类型；结构类型 struct；联合类型 union；枚举类型 enum
// 指针类型： char *；int*；int **
// 整型的打印： %d 输出一个有符号的10进制int类型；%o 输出8进制的int类型；%x 输出16进制的int类型，字母以小写输出；%X 输出16进制的int类型，字母以大写输出；
// %u 输出一个10进制的无符号数
// 定义8进制数据以0开头；定义16进制数据，以0x开头；C语言不能直接书写二进制数
//
//
//
//

#include <stdio.h>
#include <stdlib.h>
int main(void)
{

    return 0;
}